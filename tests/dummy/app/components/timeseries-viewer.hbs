<div>

  <h1>{{ title }}</h1>

   {{ fusioncharts-xt
    width=this.width
    height=this.height
    type=this.type
    dataFormat=this.dataFormat
    dataSource=this.dataSource
  }}

  <button {{on "click" this.onClick}}>Change Chart</button>

</div>